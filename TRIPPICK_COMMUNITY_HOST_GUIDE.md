# TRIPPICK 고객 커뮤니티 ↔ 호스트 콘솔 연동 가이드

## 1. 구현 목표

고객 사이트의 상담게시판·리뷰게시판과 `trippick-host` 관리자 화면을 동일한 Supabase 데이터로 연결했다. 고객이 로그인 후 글을 작성하면 관리자 화면에 표시되고, 관리자가 답변을 저장하면 다시 고객 게시판에 노출된다.

이번 작업은 GitHub Pages 운영 배포와 분리해 검토할 수 있도록 소스 파일 변경만 준비한다. 별도 브랜치를 병합하기 전에는 운영 페이지 코드가 바뀌지 않는다.

## 2. 전체 데이터 흐름

```text
고객 로그인
  ├─ 상담 작성 → public.board_posts
  │                └─ 호스트 답변 → admin_answer / status=answered
  │                                      └─ 고객 상담게시판에 답변 표시
  └─ 리뷰 작성 → public.reviews
                   └─ 호스트 답글 → admin_reply
                                         └─ 고객 리뷰게시판에 답글 표시
```

고객·관리자 클라이언트 모두 브라우저에는 Supabase publishable key만 사용한다. 읽기·쓰기의 최종 허용 여부는 Postgres RLS가 로그인 사용자의 ID와 관리자 역할을 확인해 판정한다. `service_role` 또는 secret key는 프론트엔드에 포함하지 않았다.

## 3. 고객 사이트 변경

### 상담게시판

- 기존 `#qnaForm`을 Supabase `board_posts` INSERT와 연결했다.
- 작성 전 로그인을 확인하고, 작성자의 `user_id`를 현재 로그인 사용자로 고정한다.
- 공개 글은 누구나 조회할 수 있고, 비밀 글은 작성자와 관리자만 조회할 수 있다.
- 관리자가 답변하면 `admin_answer`와 `answered` 상태가 고객 화면에 표시된다.
- 로컬 예시 목록이 아닌 실제 DB 결과가 최종 목록을 구성한다.

### 리뷰게시판

- 리뷰게시판 안에서 바로 열 수 있는 작성 폼을 추가했다.
- 캠핑장, 1~5점 별점, 제목, 본문을 입력해 `reviews`에 저장한다.
- 등록 성공 후 공개 목록을 다시 불러오고 작성 폼을 닫는다.
- 호스트가 남긴 `admin_reply`를 각 리뷰 아래의 별도 답글 블록으로 표시한다.
- DB 결과가 없을 때는 실제 빈 상태를 표시한다.

### 호스트 센터 이동

- 전 페이지 공통 계정 메뉴와 모바일 메뉴에 `호스트 센터` 링크를 자동 추가했다.
- 호스트 콘솔의 `고객 화면 미리보기` 링크는 잘못된 호스트 리다이렉트가 아니라 고객 홈 `../index.html`을 열도록 수정했다.

## 4. `trippick-host` 관리자 변경

### 관리자 인증 가드

- 로그인 세션이 없으면 관리자 로그인 화면을 표시한다.
- 로그인 후 `profiles.role = 'admin'`인지 확인한다.
- 관리자가 아니면 데이터 UI를 열지 않으며 다시 로그아웃 처리한다.
- 관리자 판정은 화면 코드만 믿지 않고 기존 RLS의 `private.is_admin()` 정책이 UPDATE 시 다시 검증한다.
- 로그인 가드가 열린 동안 배경 콘솔을 `inert`·`aria-hidden` 처리해 키보드와 보조기기 접근을 막는다.
- `비밀번호를 잊으셨나요?`에서 Supabase 재설정 메일을 요청할 수 있다.
- 이메일의 복구 링크로 돌아오면 새 비밀번호와 확인값을 입력해 `updateUser()`로 변경하고, 변경 완료 후 재로그인한다.
- 로컬에서 메일을 요청해도 복구 링크는 `publicSiteUrl`의 실제 GitHub Pages 호스트 페이지로 돌아오므로 `127.0.0.1` 주소를 사용자에게 보내지 않는다.

### 상담 관리

- `board_posts` 최신 200건을 조회한다.
- 답변 대기·완료·이번 달 문의·답변률 통계를 실제 데이터로 계산한다.
- 문의 목록에서 작성자, 카테고리, 공개 상태, 작성일을 확인한다.
- 답변 등록·수정 시 `admin_answer`, `status`, `updated_at`을 함께 저장한다.
- 관리자가 글을 숨기거나 다시 공개할 수 있다.

### 리뷰 관리

- `reviews` 최신 200건을 조회한다.
- 평균 평점, 전체 리뷰, 미답변 리뷰, 사진 리뷰 통계를 실제 데이터로 계산한다.
- 전체·미답변·사진·낮은 평점 필터, 텍스트 검색, 정렬을 제공한다.
- 답글 등록·수정 시 `admin_reply`, `updated_at`을 저장한다.
- 리뷰 공개·숨김 상태를 변경할 수 있다.

### 갱신 방식

- 페이지 진입 시 즉시 조회한다.
- 탭으로 다시 돌아왔을 때와 30초 간격으로 자동 갱신한다.
- 현재 Supabase Realtime publication에는 해당 두 테이블이 등록되어 있지 않으므로, 별도 DB 마이그레이션 없이도 동작하는 안전한 폴링 방식을 사용했다.

## 5. 사용 중인 DB와 보안 정책

기존 Supabase 프로젝트의 다음 구조를 그대로 사용했으며 이번 작업에서 운영 DB 스키마를 변경하지 않았다.

| 테이블 | 고객 INSERT | 고객 SELECT | 관리자 UPDATE |
|---|---|---|---|
| `board_posts` | 본인 `user_id`만 | 공개 글 또는 본인 비밀 글 | `private.is_admin()` |
| `reviews` | 본인 `user_id`만 | `visible` 또는 본인 글 | `private.is_admin()` |
| `profiles` | 사용 안 함 | 본인 또는 관리자 | 사용 안 함 |

RLS가 활성화되어 있고 UPDATE 정책에는 `USING`과 `WITH CHECK`가 모두 있다. UPDATE에 필요한 SELECT 정책도 존재한다. 관리자 여부는 사용자가 수정 가능한 `user_metadata`가 아니라 DB의 관리자 판정 함수를 사용한다.

## 6. 주요 변경 파일

- `pages/community-reviews.html`: 커뮤니티 리뷰 직접 작성 폼
- `js/supabase-app.js`: 상담·리뷰 INSERT/SELECT 및 호스트 답변 렌더링
- `js/script.js`: 고객 사이트의 호스트 센터 공통 링크
- `css/styles.css`: 리뷰 작성·호스트 답글·모바일 링크 스타일
- `trippick-host/admin-data.js`: 관리자 인증, 조회, 통계, 답변, 상태 관리
- `trippick-host/admin.js`: Supabase 관리자 모듈 로더
- `trippick-host/admin.css`: 로그인 가드·동적 목록·토스트·반응형 스타일
- `trippick-host/admin-*.html`: 고객 미리보기 링크 경로 수정

## 7. 운영 전 확인 방법

1. 고객 사이트에서 일반 회원으로 로그인한다.
2. 상담게시판에서 문의를 등록한다.
3. 리뷰게시판에서 캠핑장·별점·제목·후기를 등록한다.
4. `trippick-host/admin-consult.html`에서 관리자 계정으로 로그인해 문의를 확인하고 답변한다.
5. `trippick-host/admin-review.html`에서 리뷰를 확인하고 답글을 등록한다.
6. 고객 상담·리뷰게시판을 새로 열어 각각의 답변이 표시되는지 확인한다.

관리자 비밀번호나 secret key는 저장소에 추가하지 않았다. 운영 확인에는 Supabase에서 이미 `admin` 역할이 지정된 계정의 로그인 정보가 필요하다.

## 8. 후속 확장 권장 사항

- 캠핑장별 호스트 멀티테넌시: `camp_id`와 호스트-캠핑장 매핑 테이블 추가
- 리뷰 사진 업로드: Supabase Storage 버킷과 사용자별 경로 RLS 추가
- 알림: 답변 완료 시 이메일 또는 웹 푸시 발송
- 감사 로그: 관리자 답변·숨김 처리의 담당자와 변경 이력 보존
- Realtime: 검토된 migration으로 두 테이블을 publication에 등록한 뒤 폴링을 구독 방식으로 교체
