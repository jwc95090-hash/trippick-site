# TRIPPICK 기능 확장 설명

## 개선 목표

기존 사이트가 `탐색 → 상세 → 예약`의 기본 흐름과 공공데이터 신뢰성을 보여주는 데 강점이 있었다면, 이번 개선은 사용자가 캠핑장을 **더 구체적으로 찾고, 같은 기준으로 비교하고, 처음 캠핑을 준비하는 과정까지 완료**하도록 만드는 데 초점을 두었습니다.

기획안의 핵심 문제인 정보 파편화, 초보자의 판단 어려움, 비교 피로, 재방문 연결 부족을 메인 화면 안에서 해결하도록 설계했습니다.

## 추가된 기능

### 1. 상세 검색

- 캠핑장명·지역·소개 문구를 함께 찾는 키워드 검색
- 2명부터 5명 이상까지 인원 조건
- 전기, 온수, 샤워실, 와이파이, 반려동물, 물놀이 시설 필터
- 화장실·샤워실·전기 정보가 모두 확인된 `초보 안심 캠핑장` 필터
- 추천순, 이름순, 최근 등록순 정렬
- 현재 적용된 조건을 검색 결과 상단 칩으로 표시
- 체크인·체크아웃을 모두 입력하도록 검증하고 숙박 일수 계산
- 결과를 8개씩 보여주고 `결과 더 보기`로 점진 로딩

공공데이터에 실시간 재고와 요금은 없으므로, 검색 결과에서는 이를 명확히 안내하고 상세 페이지와 캠핑장 문의로 연결합니다.

### 2. 캠핑장 비교함

- 검색 결과에서 최대 3곳 선택
- 화면 하단 비교함에 선택 상태 유지
- 위치, 유형, 주요 시설, 반려동물, 초보 추천, 연락처를 표 형태로 비교
- 비교표에서 각 캠핑장 상세 페이지로 이동

비교 기준을 고정해 사용자가 여러 상세 페이지를 오가며 기억해야 하는 부담을 줄였습니다.

### 3. 트립픽 취향 매치

다음 세 질문으로 추천 점수를 계산합니다.

1. 선호 풍경: 숲·계곡 / 바다·노을 / 산·전망
2. 숙박 방식: 직접 텐트 / 글램핑 / 카라반
3. 동행: 커플 / 가족 / 반려동물

선택값을 고캠핑 데이터의 시설 유형, 소개, 주소, 편의시설, 반려동물 정보와 대조해 상위 3곳을 추천합니다. 이미지 보유 여부와 초보 안심 조건도 보조 점수로 사용합니다.

### 4. 첫 캠핑 준비도

- 캠핑장·날짜, 텐트·침구, 취사, 안전, 정책 확인의 5단계 체크리스트
- 완료율을 원형 진행률로 표시
- 진행 정도에 맞는 상태 메시지 제공
- 체크 상태를 `localStorage`에 저장해 새로고침 후에도 유지
- 초보 캠핑 가이드로 연결

예약 이후의 준비 경험까지 서비스 안에 남겨 재방문 이유를 만듭니다.

### 5. 최근 본 캠핑장

- 공공데이터 캠핑장 상세 페이지 방문 시 최근 기록 저장
- 홈에서 최대 6곳을 가로 스크롤 카드로 노출
- 기록 전체 삭제 제공
- 다시 선택하면 같은 캠핑장 상세 데이터 복원

### 6. 반응형·접근성 보강

- 모바일에서는 추천 선택지를 세로형 터치 카드로 전환
- 상세 필터를 1열 레이아웃으로 재배치
- 비교함을 모바일 하단 탭 위에 표시
- 작은 화면에서는 우측 퀵메뉴를 아이콘형으로 축소해 폼 가림 방지
- 모든 신규 입력 요소에 연결된 레이블과 키보드 포커스 제공
- 검색 상태, 선택 개수, 준비율을 `aria-live`로 안내
- 날짜 입력의 최소 날짜를 오늘로 제한

## 데이터와 상태 구조

| 기능 | 데이터 원천 | 저장 방식 |
| --- | --- | --- |
| 검색·추천·비교 | 한국관광공사 고캠핑 API | 현재 세션 메모리 |
| 상세 페이지 전달 | 선택한 고캠핑 데이터 | `sessionStorage` |
| 준비도 | 사용자 체크 상태 | `localStorage` |
| 최근 본 캠핑장 | 최근 방문한 데이터 요약 | `localStorage` |
| 찜·리뷰 | 기존 사이트 데이터 레이어 | `localStorage` |

## 실제 서비스 전환 시 필요한 작업

현재 구현은 정적 GitHub Pages에서도 기능 흐름을 체험할 수 있는 프런트엔드 프로토타입입니다. 상용 서비스에서는 아래 작업이 필요합니다.

1. 공공데이터 API 키를 서버 또는 Edge Function으로 이동
2. 캠핑장별 객실·사이트 재고와 날짜별 요금 데이터 모델 추가
3. 회원 계정에 찜, 최근 본 항목, 준비도 동기화
4. 비교·검색 이벤트 분석을 위한 행동 로그 설계
5. 호스트가 시설 정보와 예약 가능 수량을 관리하는 DB 연동
6. 결제 승인 결과와 예약 확정 상태를 서버에서 원자적으로 처리
7. 취향 추천 점수를 클릭·예약 데이터 기반 랭킹 모델로 고도화

## 확인한 동작

- JavaScript 문법 검사 통과
- 상세 검색 결과 8개 렌더링 및 16개로 더 보기 확인
- 캠핑장 2곳 선택 후 비교표 생성 확인
- 취향 매치 상위 3곳 출력 확인
- 준비도 2개 선택 시 40% 계산 및 저장 확인
- 상세 방문 후 홈의 최근 본 캠핑장 노출 확인
- 390px 모바일 화면에서 가로 스크롤 없음 확인
- 브라우저 콘솔 오류 없음 확인
